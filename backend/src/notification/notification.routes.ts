/**
 * Notification service — placeholder for Sprint 1.
 * FCM/APNs push on GeofenceBreachDetected lands in Sprint 3 (Issue #9).
 */
import { Router } from 'express';

export const notificationRouter = Router();
// Sprint 3: FCM/APNs push consumer.